pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "My second pipeline"'
                sh 'date'
            }
        }
    }
}
